services:
  gstreamer-env:
    image: gstreamer-dev
    build: .
    container_name: celes-gstreamer-container
    volumes:
      - .:/workspace
    environment:
      - DISPLAY=host.docker.internal:0.0
      - XDG_RUNTIME_DIR=/tmp
    stdin_open: true
    tty: true
    working_dir: /workspace